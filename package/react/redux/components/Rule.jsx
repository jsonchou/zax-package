import React, { PureComponent } from 'react'

export default class Rule extends PureComponent {
    render() {
        return (
            <div className="Rule">
                活动规则
            </div>
        );
    }
}